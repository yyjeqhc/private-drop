[package]
name = "webcodex-tool-contracts"
version.workspace = true
license.workspace = true
edition.workspace = true

[features]
default = []
root-test-support = []
workspace-checkpoints = []

[dependencies]
webcodex-core = { path = "../webcodex-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
