use super::RouteAuth::AuthMiddleware;
use super::{
    route, AuditClass::*, RouteId::*, RouteMethod::*, RouteOpenApiProjection::*, RouteSpec,
    RouteSurface::*,
};
use webcodex_core::authority::{
    OAuthRouteScopePolicy::*, SCOPE_JOB_RUN, SCOPE_PROJECT_READ, SCOPE_PROJECT_WRITE,
    SCOPE_RUNTIME_READ,
};

// Project Connector. Four recovery/discovery routes historically had no
// ordinary delegated scope entry; BootstrapOnly makes that existing
// fail-closed behavior explicit while project-credential handling remains
// owned by the specialized Connector gate.
pub(super) const ROUTES: &[RouteSpec] = &[
    route(
        ConnectorReadiness,
        Post,
        "/api/connector/readiness",
        BootstrapOnly,
        Connector,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskStart,
        Post,
        "/api/connector/task/start",
        Require(SCOPE_RUNTIME_READ),
        Connector,
        ConnectorCapability("task_start"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskList,
        Post,
        "/api/connector/task/list",
        BootstrapOnly,
        Connector,
        ConnectorCapability("task_list"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskResume,
        Post,
        "/api/connector/task/resume",
        BootstrapOnly,
        Connector,
        ConnectorCapability("task_resume"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorFilesList,
        Post,
        "/api/connector/files/list",
        BootstrapOnly,
        Connector,
        ConnectorCapability("files_list"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorFilesRead,
        Post,
        "/api/connector/files/read",
        Require(SCOPE_PROJECT_READ),
        Connector,
        ConnectorCapability("files_read"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorFilesSearch,
        Post,
        "/api/connector/files/search",
        Require(SCOPE_PROJECT_READ),
        Connector,
        ConnectorCapability("files_search"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorCodeNavigate,
        Post,
        "/api/connector/code/navigate",
        Require(SCOPE_PROJECT_READ),
        Connector,
        ConnectorCapability("code_navigate"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorCodeImpact,
        Post,
        "/api/connector/code/impact",
        Require(SCOPE_PROJECT_READ),
        Connector,
        ConnectorCapability("code_impact"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorEditsApply,
        Post,
        "/api/connector/edits/apply",
        Require(SCOPE_PROJECT_WRITE),
        Connector,
        ConnectorCapability("edits_apply"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorChecksRun,
        Post,
        "/api/connector/checks/run",
        Require(SCOPE_JOB_RUN),
        Connector,
        ConnectorCapability("checks_run"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorCommandsRun,
        Post,
        "/api/connector/commands/run",
        Require(SCOPE_JOB_RUN),
        Connector,
        ConnectorCapability("commands_run"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskReview,
        Post,
        "/api/connector/task/review",
        Require(SCOPE_PROJECT_READ),
        Connector,
        ConnectorCapability("task_review"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskCancel,
        Post,
        "/api/connector/task/cancel",
        Require(SCOPE_JOB_RUN),
        Connector,
        ConnectorCapability("task_cancel"),
        Other,
        AuthMiddleware,
    ),
    route(
        ConnectorTaskFinish,
        Post,
        "/api/connector/task/finish",
        Require(SCOPE_PROJECT_WRITE),
        Connector,
        ConnectorCapability("task_finish"),
        Other,
        AuthMiddleware,
    ),
];
