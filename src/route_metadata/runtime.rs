use super::RouteAuth::AuthMiddleware;
use super::{
    openapi, route, AuditClass::*, RouteId::*, RouteMethod::*, RouteOpenApiProjection::*,
    RouteSpec, RouteSurface::*,
};
use webcodex_core::authority::{
    OAuthBodyAwarePolicy, OAuthRouteScopePolicy::*, SCOPE_JOB_RUN, SCOPE_PROJECT_READ,
    SCOPE_PROJECT_WRITE, SCOPE_RUNNER_MANAGE, SCOPE_RUNTIME_READ,
};

pub(super) const ROUTES: &[RouteSpec] = &[
    route(
        ToolsList,
        Post,
        "/api/tools/list",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        PublicAction(openapi::LIST_RUNTIME_TOOLS),
        Other,
        AuthMiddleware,
    ),
    route(
        ToolsCall,
        Post,
        "/api/tools/call",
        BodyAware(OAuthBodyAwarePolicy::RuntimeToolCall),
        RuntimeApi,
        PublicAction(openapi::CALL_RUNTIME_TOOL),
        Command,
        AuthMiddleware,
    ),
    route(
        ArtifactsImport,
        Post,
        "/api/artifacts/import",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::IMPORT_CONVERSATION_FILES_TO_PROJECT),
        Artifact,
        AuthMiddleware,
    ),
    route(
        JobsStop,
        Post,
        "/api/jobs/stop",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        JobsList,
        Post,
        "/api/jobs/list",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        PublicAction(openapi::LIST_RUNTIME_JOBS),
        Other,
        AuthMiddleware,
    ),
    route(
        JobsTail,
        Post,
        "/api/jobs/tail",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        PublicAction(openapi::GET_RUNTIME_JOB_TAIL),
        Job,
        AuthMiddleware,
    ),
    route(
        RunnerConfigCheck,
        Post,
        "/api/runners/config/check",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RunnerConfigReload,
        Post,
        "/api/runners/config/reload",
        Require(SCOPE_RUNNER_MANAGE),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ProjectsList,
        Post,
        "/api/projects/list",
        Require(SCOPE_PROJECT_READ),
        RuntimeApi,
        PublicAction(openapi::LIST_PROJECTS),
        Context,
        AuthMiddleware,
    ),
    route(
        ProjectsRegister,
        Post,
        "/api/projects/register",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::REGISTER_PROJECT),
        Other,
        AuthMiddleware,
    ),
    route(
        ProjectsCreate,
        Post,
        "/api/projects/create",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::CREATE_PROJECT),
        Other,
        AuthMiddleware,
    ),
    route(
        ProjectsUnregister,
        Post,
        "/api/projects/unregister",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ProjectsResolveOrRegister,
        Post,
        "/api/projects/resolve-or-register",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ProjectsGitStatus,
        Post,
        "/api/projects/git_status",
        Require(SCOPE_PROJECT_READ),
        RuntimeApi,
        PublicAction(openapi::GET_PROJECT_GIT_STATUS),
        Git,
        AuthMiddleware,
    ),
    route(
        ProjectsListFiles,
        Post,
        "/api/projects/list_files",
        Require(SCOPE_PROJECT_READ),
        RuntimeApi,
        PublicAction(openapi::LIST_PROJECT_FILES),
        Context,
        AuthMiddleware,
    ),
    route(
        ProjectsApplyUnifiedDiff,
        Post,
        "/api/projects/apply_unified_diff",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::APPLY_UNIFIED_DIFF),
        Edit,
        AuthMiddleware,
    ),
    route(
        ProjectsRunShell,
        Post,
        "/api/projects/run_shell",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        PublicAction(openapi::RUN_PROJECT_SHELL_COMMAND),
        Shell,
        AuthMiddleware,
    ),
    route(
        ProjectsGitRestorePaths,
        Post,
        "/api/projects/git_restore_paths",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::GIT_RESTORE_PATHS),
        Artifact,
        AuthMiddleware,
    ),
    route(
        ProjectsDiscardUntracked,
        Post,
        "/api/projects/discard_untracked",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        PublicAction(openapi::DISCARD_UNTRACKED_FILES),
        Artifact,
        AuthMiddleware,
    ),
    route(
        ProjectsRunJob,
        Post,
        "/api/projects/run_job",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        PublicAction(openapi::START_PROJECT_SHELL_JOB),
        Job,
        AuthMiddleware,
    ),
    route(
        RuntimeStatus,
        Post,
        "/api/runtime/status",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        PublicAction(openapi::GET_RUNTIME_STATUS),
        Report,
        AuthMiddleware,
    ),
];

pub(super) const SHELL_ROUTES: &[RouteSpec] = &[
    route(
        ShellRun,
        Post,
        "/api/shell/run",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        Hidden,
        Shell,
        AuthMiddleware,
    ),
    route(
        ShellFile,
        Post,
        "/api/shell/file",
        Require(SCOPE_PROJECT_WRITE),
        RuntimeApi,
        Hidden,
        Shell,
        AuthMiddleware,
    ),
    route(
        ShellJob,
        Post,
        "/api/shell/job",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        Hidden,
        Shell,
        AuthMiddleware,
    ),
    route(
        ShellJobsStatus,
        Post,
        "/api/shell/jobs/status",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ShellJobsLog,
        Post,
        "/api/shell/jobs/log",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ShellJobsStop,
        Post,
        "/api/shell/jobs/stop",
        Require(SCOPE_JOB_RUN),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        ShellJobsList,
        Post,
        "/api/shell/jobs/list",
        Require(SCOPE_RUNTIME_READ),
        RuntimeApi,
        Hidden,
        Other,
        AuthMiddleware,
    ),
];
